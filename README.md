# Makespan

A job shop scheduling backend built on OR-Tools CP-SAT and FastAPI.

## Install

```
uv sync
```

## Run tests

```
uv run pytest
```

## Run the dev server

```
uv run uvicorn makespan.main:app --reload
```
